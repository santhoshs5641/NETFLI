import 'package:flutter/material.dart';

class Screen_search extends StatelessWidget {
  const Screen_search({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text("screen search"),);
  }
}